{
  "name": "@mmldigi/forms-embed",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mmldigi/forms-core": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "esbuild": "^0.21.0",
    "happy-dom": "^14.0.0",
    "vitest": "1.5.3"
  }
}
